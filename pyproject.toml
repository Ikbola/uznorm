[project]
name = "uznorm"
version = "0.1.0"
description = "Normalize Uzbek text — apostrophes, transliteration, whitespace"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/uznorm"]